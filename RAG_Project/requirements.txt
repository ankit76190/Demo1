# Core RAG dependencies
sentence-transformers>=2.7.0   # Free local embedding model (no API key needed)
chromadb>=0.5.0                # Local vector database

# Optional: use OpenAI models instead of local ones
openai>=1.30.0

# Utilities
numpy>=1.24.0
